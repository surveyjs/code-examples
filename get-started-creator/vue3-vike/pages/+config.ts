import type { Config } from 'vike/types'
import vikeVue from 'vike-vue/config'

const config: Config = {
  title: 'SurveyJS Creator v3 × Vike',
  description: 'SurveyJS Creator v3 integration demo in a Vike + Vue app',
  extends: [vikeVue],
}

export default config
