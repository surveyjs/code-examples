import type { Config } from 'vike/types'
import vikeVue from 'vike-vue/config'

const config: Config = {
  title: 'SurveyJS v3 × Vike',
  description: 'SurveyJS Form Library v3 integration demo in a Vike + Vue app',
  extends: [vikeVue],
}

export default config
